# @genetic-assembly/inspector

Mount the local study inspector with `mountInspector(element, { baseUrl, onPreview, onExport, onReplay })`. Call `dispose()` on removal. The companion serves the same interface at its root URL. Search, validation, baseline and replay measurements retain their own phases and seeds.
