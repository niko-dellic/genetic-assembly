{
  "name": "@genetic-assembly/inspector",
  "version": "0.4.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json && esbuild src/index.ts --bundle --minify --format=esm --platform=browser --outfile=public/inspector.js && esbuild src/grabm.ts --bundle --minify --format=esm --platform=browser --outfile=public/grabm.js"
  },
  "dependencies": {
    "@genetic-assembly/sdk": "file:../sdk"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "esbuild": "^0.25.0"
  }
}
