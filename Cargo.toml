[package]
name = "genetic-assembly"
version = "0.1.0"
edition = "2021"
description = "WebAssembly-based multi-objective optimization solver using NSGA-II algorithm with support for parallel execution"
repository = "https://github.com/nikodellic/genetic-assembly"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde-wasm-bindgen = "0.6"
js-sys = "0.3"
getrandom = { version = "0.2", features = ["js"] }
rand = { version = "0.8", features = ["small_rng"] }
rand_distr = "0.4"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[profile.release]
opt-level = 3
lto = true
