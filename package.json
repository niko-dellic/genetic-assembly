{
  "name": "genetic-assembly",
  "version": "1.0.0",
  "description": "WebAssembly-based Multi-Objective Optimization",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build --target web --out-dir pkg --release",
    "dev": "npm run build:wasm && cd demo && npm run dev",
    "demo:install": "cd demo && npm install",
    "demo:dev": "cd demo && npm run dev",
    "demo:build": "npm run build:wasm && cd demo && npm run build",
    "demo:preview": "cd demo && npm run preview"
  },
  "keywords": [
    "wasm",
    "genetic-algorithm",
    "nsga2",
    "nsga3",
    "multi-objective-optimization"
  ],
  "author": "nikodellic",
  "license": "MIT"
}
