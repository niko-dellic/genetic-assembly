# Keep the workspace native by default. Browser clients talk to the native Rust
# server; WASM builds, when reintroduced, must opt into their target explicitly.
[alias]
test-native = "test --workspace"
